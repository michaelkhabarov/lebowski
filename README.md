# Telegram bot that shows how much usd cash is available in Tinkkoff atms in Saint-Petersburg

To start:

```bash
git clone git@github.com:michaelkhabarov/lebowski.git
cd lebowski
bundle install
bundle exec rake db:migrate
bundle exec bin/bot
```
